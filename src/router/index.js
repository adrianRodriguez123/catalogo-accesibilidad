import Vue from 'vue'
import Router from 'vue-router'

Vue.use(Router)

const routes = [
  {
    path: '/',
    name: 'home',
    component: Router
  },
  {
    
  }
]

const router = new Router({
  
})

export default router
